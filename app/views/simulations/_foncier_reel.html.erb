<%# Le foncier réel n'impose que le loyer hors charges, les charges réelles et les intérêts déduits. %>
<p class="section-hint"><%= t("views.simulations.show.foncier_reel_hint") %></p>

<%= render "simulations/projection", projection: projections[:foncier_reel] %>
