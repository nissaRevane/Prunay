<%#
  Le tableau de bord : la place que le simulateur prendra. Tant qu'il n'existe pas, la page
  le dit plutôt que de montrer des tuiles vides.
%>
<div class="page-header">
  <h1><%= t("views.dashboard.title") %></h1>
</div>

<p class="empty-state"><%= t("views.dashboard.empty") %></p>
